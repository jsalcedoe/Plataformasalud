1. git add .
2. git commit -m "mensaje relacionado"
3. git push -u origin master // para subir los cambios confirmados con el commit

con el comando git status -s podemos ver los cambios que se presentan en el directorio
